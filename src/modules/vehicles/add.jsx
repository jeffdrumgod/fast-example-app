const propTypes = {
	location: PropTypes.object
};

const defaultProps = {
	location: {}
};

const VehiclesAdd = () => (
	<div>Teste Add</div>
);

VehiclesAdd.propTypes = propTypes;
VehiclesAdd.defaultProps = defaultProps;

export default VehiclesAdd;
